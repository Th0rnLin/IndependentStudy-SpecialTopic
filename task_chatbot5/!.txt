我把受傷人數回復改成，"我們預計派多少輛救護車"這樣，這部分還可以討論

緊急救護的回復的部分，我再case.py裡面新增一個qakeyword.txt這個檔案，句子與這個字典去做比對，然後回復關鍵字
e.g. 輸入：我這邊剛剛發生車禍 => 回應：請問是車禍嗎

然後我新增 mode ，用來輸入 rum 和 interrupt，這樣就可以不用重載資料庫，不過需要 C# 來傳遞參數執行
e.g. 有三個 bottom 分別是接聽、中斷、重載，接聽指的是 run，中斷指的是 interrupt，重載指的是重新執行這個檔案

※這次的檔案裏面就沒有word2vec的檔案，你需要自己新增，我的路徑是"database/word2vec_data/word2vec.model"